<h1 class="tienda-title">VENTAS <i class="fas fa-store"></i></h1>
    <div class="carrito-subtitulo">
        <h2>TUS PRODUCTOS</h2>
    </div>
    <section class="section-tabla-carrito">
        <table class="table" id="table">
            <thead class="tabla-encabezado">
                <tr class="tabla-encabezado2">
                    <th>USUARIO</th>
                    <th>DESCRIPCION</th>
                    <th>CANTIDAD</th>
                    <th>PRECIO</th>
                </tr>
            </thead>
            <tbody>
                {{#each venta}}
                <tr>
                    <td>
                        <p>{{user_id}}</p>
                    </td>
                    <td>
                        <span class="carrito-precio">{{nombre}}</span>
                    </td>
                    <td>
                        <span class="carrito-precio">{{cantidad}}</span>
                    </td>
                    <td class="ventas-btncont">
                        <span class="carrito-precio">${{precio}}</span>
                        <a href="/tienda/entregarventa/{{id}}" class="btn btn-danger btn-entrega">Entregar venta</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
            <tfoot></tfoot>
        </table>      
    </section>